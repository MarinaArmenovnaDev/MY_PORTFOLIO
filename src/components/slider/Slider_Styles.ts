import styled from "styled-components";

const Slider = styled.div`
    display: flex;
    justify-content: center;
    align-items: center;
    height: 100%;
    position: relative;
    z-index: 1;
    margin-top: 60px;

`

export const S ={
    Slider
}